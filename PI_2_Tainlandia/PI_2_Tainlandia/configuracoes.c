#include <stdio.h>
#include <allegro5/allegro.h>
#include "configuracoes.h"



// Função de configuração (vazia por enquanto)
void configuracoes(ALLEGRO_EVENT_QUEUE* event_queue) {
    // Implementação futura
    printf("Abriu configurações!\n");
    //ALLEGRO_BITMAP*menu = al_load_bitmap("configImg.png");

    //int botao_mute_x = 415;
    //int botao_mute_y = 330; // Coordenadas do botão "mute"
    /*
    if (mouse_sobre_botao(mouse_x, mouse_y, botao_play_x, botao_play_y, largura_botao, altura_botao)) {
        iniciar_jogo = true;
        rodando = false;  // Para sair do loop e iniciar o jogo
    }
   */
}
